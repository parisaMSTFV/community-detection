# Security and publication boundary

The default project runs offline and has no credential, network, database, model, or dashboard dependency. CI scans tracked text for common credential and private-infrastructure patterns.

Do not add real user identifiers, category taxonomies, graph exports, queries, private URLs, screenshots, credentials, or production configuration to this repository.
